package service;

import models.Ticket;

public interface TicketService {
    /*Ticket Buy(Ticket seat, int start, int end, String name, String surname);
    default void getDistance(int start, int end){

    }
    default void getPrice(Ticket ticket, int distance) {
        switch (ticket.getSeat().getTrain().getType()) {
            case "Night": {
                 ticket.setPrice(distance / 10);
            }
            case "Day": {
                ticket.setPrice(distance / 10);
            }
            case "InterCity":{
                ticket.setPrice(distance / 10 * 2);
            }
            default: ticket.setPrice(0);;
        }
    }*/
}
